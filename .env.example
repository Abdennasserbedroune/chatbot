# Groq API Configuration
# Get your free API key from: https://console.groq.com/keys
# Free tier limits: ~30 requests per minute
GROQ_API_KEY=your_groq_api_key_here

# Optional: Configure model (default: llama-3.1-70b-versatile)
# Available models: llama-3.1-70b-versatile, llama-3.1-8b-instant, mixtral-8x7b-32768, etc.
# See: https://console.groq.com/docs/models
GROQ_MODEL=llama-3.1-70b-versatile

# Optional: Configure API timeout in milliseconds (default: 30000)
GROQ_TIMEOUT=30000

# Optional: Configure max retries for transient errors (default: 3)
GROQ_MAX_RETRIES=3

# Optional: Configure initial retry delay in milliseconds (default: 1000)
# Delays use exponential backoff: delay * 2^retryCount
GROQ_INITIAL_RETRY_DELAY=1000
